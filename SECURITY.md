# Security Policy

Football Workbench 处理现场参与者信息、工作人员会话、官方资源 Code 和活动状态。可能导致未授权访问、资源泄露、状态破坏或部署接管的问题，应作为安全问题私下报告。

项目仍处于 `0.1.x` 阶段，没有声称通过独立安全审计，也不提供固定响应或修复 SLA。

## 私下报告漏洞

请优先使用 GitHub 仓库的 **Security** 页面，通过 **Report a vulnerability** 创建私有报告：

- [GitHub Security Advisories 私有报告入口](https://github.com/ratio-dd/agentic-football-event-ops/security/advisories/new)

如果仓库尚未启用 Private vulnerability reporting，或该入口对你不可用，请通过维护者的 [GitHub profile](https://github.com/ratio-dd) 联系维护者，先约定私下沟通渠道。不要在公开 Issue、Discussion、Pull Request、提交信息或社交媒体中披露漏洞细节。

报告中请尽量提供：

- 受影响的版本、Git SHA、部署模式和相关配置假设；
- 影响范围，以及攻击者需要具备的角色、网络位置或已有权限；
- 最小复现步骤、请求/响应摘要和预期结果；
- 已脱敏的日志、截图或概念验证；
- 是否已经观察到真实数据或凭证暴露。

请不要为证明问题而访问不属于你的参与者数据、使用真实 Code/PIN、破坏活动状态或攻击正在运行的活动环境。

维护者会在可用时间内尽力确认、复现、评估影响并讨论修复与披露方式，但不承诺固定的首次响应、修复或发布日期。安全修复默认面向当前公开默认分支；旧部署快照、私有分支和自行修改的实例需要由各自运营方评估。

## 重点威胁面

以下清单描述报告范围，不代表这些区域已经完成安全审计：

| 区域 | 需要关注的问题 |
| --- | --- |
| 角色与会话 | 参与者浏览器身份与昵称恢复、Staff PIN 会话、Admin 提权、服务端权限校验、会话复制或长期存留、角色间数据越权。 |
| QR 与 Code 生命周期 | 参与者 QR 的内容和公开读取边界；Workshop/Game Portal Code 的导入、分配、展示、补发和回收；错误队伍绑定或跨参与者泄露。 |
| 参与者输入 | 昵称、反馈和其他表单输入引发的脚本注入、日志污染、过量数据、隐私暴露或状态绕过。 |
| API 与维护接口 | 匿名、参与者、Staff、Admin 和 display API 的字段隔离，以及维护快照的暴露边界。 |
| Shell、Docker 与 SSH 部署 | `lightsail/` 中的构建和部署脚本、强制 SSH 命令、归档解析、镜像来源、Compose/Caddy 边界、volume 权限、回滚和命令注入。 |
| 依赖与第三方贡献 | npm、容器镜像、GitHub Actions 和外部贡献中的供应链风险、锁文件完整性、恶意生成物或未经说明的依赖变更。 |
| 凭证与活动数据 | Staff/Admin PIN、官方访问码、GitHub/AWS/SSH 凭证、TLS 私钥、环境变量、SQLite 文件、备份、日志和截图的意外提交或公开。 |

## 代码漏洞与部署配置

代码漏洞包括但不限于：认证或授权绕过、跨角色数据泄露、输入或路径注入、资源重复分配、并发写入破坏状态，以及仓库脚本造成的命令执行或供应链风险。即使只在特定配置下触发，只要仓库默认行为或文档会促成该问题，也请私下报告。

部署配置问题包括但不限于：错误开放公网入口、缺失或弱化 Caddy/Basic Auth/TLS、弱 PIN、复用 feedback/live 凭证或 volume、宽松的 SSH/IAM/Docker 权限、不安全的 `.env`/SQLite/备份权限，以及 DNS、证书或日志系统泄露敏感值。

如果问题只存在于某个自行维护的实例且仓库默认配置没有参与，请先联系该实例运营方；若不确定，仍可按上述私下路径报告并说明部署差异。不要把部署健康检查通过视为应用权限、安全或业务完整性已经验证。

## 不属于漏洞报告的事项

- 不包含安全影响的一般功能缺陷、可用性建议或 roadmap 请求；
- 仅指出项目没有某项未来能力，而没有可复现安全影响；
- 需要对第三方系统或真实活动进行未授权测试才能成立的报告。

普通缺陷可以使用公开 Issue，但其中不得包含真实参与者数据、凭证、PIN、官方 Code、访问码、生产配置或未公开的漏洞细节。
